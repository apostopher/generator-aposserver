language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-aposserver' ]; then cd .. && eval "mv $currentfolder generator-aposserver" && cd generator-aposserver; fi

